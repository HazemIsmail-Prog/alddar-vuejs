# HVAC Field Service — Vue SPA

Vue 3 + Vite + Pinia + Vue Router + Tailwind + shadcn-vue-style UI.

```bash
npm install
npm run dev
```

Uses `VITE_API_URL` (default `http://localhost:8000`) with Sanctum cookie auth.
